package com.neotech.lesson15;

public class Recap15 {

	/*
	 * Class 15
--------------------------------
Class and Objects in Java

Class is a blueprint/template in which we describe the features/attributes and the behavior of future objects.

In software we define features using variables and actions/behaviors using methods.

Object is an instance of the class.
--------------------------------
Methods in Java:
	With parameters
	Without parameters

Parameters VS Arguments
When we create a method we specify the parameters that the method accepts.

void greet2(String name) 
{ // String name -> The parameter is of type String
	System.out.println("Hi " + name);
	System.out.println("Bye " + name);
}

When you call/execute this method you have to provide the argument
object.greet2("James"); // The argument is "James"
--------------------------------
Methods in Java:
	Methods that return a value --> We have to specify the datatype that will be returned
	Methods that don't return a value --> We use void keyword
--------------------------------
If we want to execute a method:

1. We need to create an Object of the class to which that method belongs
2. ONLY after that we can call method by passing valid arguments

To make a method to return value 
1. we have to use return keyword inside method body
2. we will have to specify return type on method header
--------------------------------

	 */
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
}
